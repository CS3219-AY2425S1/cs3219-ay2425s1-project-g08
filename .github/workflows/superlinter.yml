name: Lint

on:
  push:
    branches:
      - test/javaDisableCheck

jobs:
  lint:
    name: Lint code base
    runs-on: ubuntu-latest

    permissions:
      contents: write
      statuses: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Super-linter
        id: linter
        uses: super-linter/super-linter@v7.1.0
        env:
          JAVA_FILE_NAME: .github/custom_checks.xml
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_JAVASCRIPT_ES: true
          VALIDATE_JSX: true
          VALIDATE_TSX: true
          VALIDATE_TYPESCRIPT_ES: true
          VALIDATE_JAVA: true
      
